export class RenderInfo {

    constructor(real, imaginary, iterations, colors) {
        this.real = real;
        this.imaginary = imaginary;
        this.iterations = iterations;
        this.colors = colors;
    }

    real;
    imaginary;
    iterations;
    colors;


}